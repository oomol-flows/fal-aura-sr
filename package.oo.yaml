name: fal_aura_sr
displayName: "%display-name%"
description: "%description%"
version: 0.0.3
scripts:
  bootstrap: |
    npm install
    poetry install --no-root
dependencies:
  upload-to-cloud: 0.0.5
  downloader: 0.1.1
icon: ""
repository: https://github.com/oomol-flows/fal-aura-sr.git
releaseNotes: hide inner block
