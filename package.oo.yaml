name: fal_aura_sr
displayName: "%display-name%"
description: "%description%"
version: 0.0.6
scripts:
  bootstrap: |
    npm install
    poetry install --no-root
dependencies:
  downloader: 0.1.1
icon: ./icon.png
repository: https://github.com/oomol-flows/fal-aura-sr.git
releaseNotes: "fix: change return type"
